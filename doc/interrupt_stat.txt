## INTERRUPT STATISTICS

1. Вход в прерывание - сохранение контекста macro asm_save_context
    40 инструкций
2. Переход в обработчик interrupt_handler
    6 инструкция
3. interrupt_handler()
    14 инструкций
4. default_ISR()
    23 инструкции
5. fm_isr_handler()
    47 инструкций
6. reload_cycle()
    24 инструкции
7. возврат из прерывания - восстановление контекста
    3 + 37 инструкции

ИТОГО:
    194 инструкции по 4 такта = 776 тактов
    грубо 1000 тактов на 48 MHz -> 21 us, что минимум в 400 раз меньше чемминимальное время измерения 10 us
    Прерывание достаточно по скорости

